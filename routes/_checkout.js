"use strict";

const express = require('express');
const router  = express.Router();

module.exports = (knex) => {
  // router.get("/", (req, res) => {
  // });

  // router.post("/", (req, res) => {
  //   });
  return router;
}
